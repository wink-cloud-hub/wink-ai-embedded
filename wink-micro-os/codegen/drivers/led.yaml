codegen_schema: "1.1"
type: led
category: output
is_actuator: true
experimental: false
default_role: binary_indicator

fields:
  gpio_pin:
    tier: advanced
    type: int
    required: true
    c: pin
  active_high:
    tier: advanced
    type: bool
    default: true
    emit: macro
    config_member: true
  role:
    tier: stable
    type: string
    emit: none

config:
  safe_off_fn: dal_led_off

# role_bindings: omitted — 1:1 binary_indicator verbs derived from roles/*.yaml
