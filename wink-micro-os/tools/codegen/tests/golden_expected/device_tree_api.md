# DEVKITC_SMOKE Device Tree & API Specification

This document is automatically generated by the Wink codegen tool from `wink-app.json`.
Do not edit this file directly.

**Default path (L1 / A+B):** use Role APIs `{name}_{verb}` below.
**Expert escape hatch (L2 / C):** `extern dal_*_t` instances + `dal_*` named APIs remain available; prefer Role wrappers unless you need pointer-passing or advanced control.

- **Board:** `esp32_devkitc`
- **Source Configuration:** `tools/codegen/tests/golden_devkitc.json`

---

## 1. Device Summary

| Device Name | Driver Type | Abstract Role | Generated API Prefix |
| :--- | :--- | :--- | :--- |
| `board_led` | `led` | `binary_indicator` | `board_led_*` (Role default; DAL escape optional) |
| `boot_button` | `button` | `binary_sensor` | `boot_button_*` (Role default; DAL escape optional) |
| `smoke_ultrasonic` | `ultrasonic` | `distance_sensor` | `smoke_ultrasonic_*` (Role default; DAL escape optional) |

---

## 2. Configuration Macros

These macros are exported in `device_tree.h` for configuration settings:

- `#define BOARD_LED_ACTIVE_HIGH true`
- `#define BOOT_BUTTON_AUTO_POLL_MS 10u`
- `#define BOOT_BUTTON_LONG_PRESS_MS 3000u`
- `#define BOOT_BUTTON_DEBOUNCE_MS 20u`
- `#define BOOT_BUTTON_EVENT_DRIVE_SOFT_POLL true`
- `#define SMOKE_ULTRASONIC_USE_RMT true`
- `#define SMOKE_ULTRASONIC_AUTO_POLL_MS 50u`

---

## 3. Device API Reference

### 3.1 Device: `board_led`

- **Driver Type:** `led`
- **Role:** `binary_indicator`

#### Available Role APIs:

- **`void board_led_activate(void)`**
  Activate/turn on the device (e.g., turn on LED).
- **`void board_led_deactivate(void)`**
  Deactivate/turn off the device (e.g., turn off LED).
- **`void board_led_toggle(void)`**
  Toggle the state of the device.



---
### 3.2 Device: `boot_button`

- **Driver Type:** `button`
- **Role:** `binary_sensor`

#### Available Role APIs:

- **`bool boot_button_is_active(void)`**
  Check if the sensor is active. Returns `false` on error.
- **`wink_status_t boot_button_is_active_status(bool *out_active)`** (⚠️ Return value must be checked)
  Contracts-honest version to check sensor status.
- **`bool boot_button_was_active(void)`**
  Detect rising-edge transition. Returns `false` on error.
- **`wink_status_t boot_button_was_active_status(bool *out_pressed)`** (⚠️ Return value must be checked)
  Contracts-honest version to detect rising-edge transition.
- **`wink_status_t boot_button_enable_events(void)`** (⚠️ Return value must be checked) — **L1 default**
  Start posting button events to the event queue (period from JSON `auto_poll_ms`).
- **`void boot_button_disable_events(void)`** — **L1 default**
  Stop posting button events.
- **`wink_status_t boot_button_start_auto_poll(uint32_t poll_ms)`** (⚠️ Return value must be checked) — L2
  Start periodic software timer with an explicit poll period.
- **`void boot_button_stop_auto_poll(void)`** — L2
  Stop periodic software timer.



---
### 3.3 Device: `smoke_ultrasonic`

- **Driver Type:** `ultrasonic`
- **Role:** `distance_sensor`

#### Available Role APIs:

- **`wink_status_t smoke_ultrasonic_request_measurement(void)`** (⚠️ Return value must be checked)
  Trigger a non-blocking measurement.
- **`float smoke_ultrasonic_read_distance(void)`**
  Get cached distance measurement (returns `-1.0f` on error).
- **`wink_status_t smoke_ultrasonic_read_distance_status(float *out_dist_cm)`** (⚠️ Return value must be checked)
  Contracts-honest get distance measurement.
- **`wink_status_t smoke_ultrasonic_enable_distance_events(void)`** (⚠️ Return value must be checked) — **L1 default**
  Start posting `WINK_EVENT_DISTANCE_READY` each completed measurement (period from JSON `auto_poll_ms`, default 50).
- **`void smoke_ultrasonic_disable_distance_events(void)`** — **L1 default**
  Stop posting distance events.



---
