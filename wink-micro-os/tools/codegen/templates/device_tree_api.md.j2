# {{ app_name.upper() }} Device Tree & API Specification

This document is automatically generated by the Wink codegen tool from `wink-app.json`.
Do not edit this file directly.

**Default path (L1 / A+B):** use Role APIs `{name}_{verb}` below.
**Expert escape hatch (L2 / C):** `extern dal_*_t` instances + `dal_*` named APIs remain available; prefer Role wrappers unless you need pointer-passing or advanced control.

- **Board:** `{{ board or 'Generic Board' }}`
- **Source Configuration:** `{{ config_source }}`

---

## 1. Device Summary

| Device Name | Driver Type | Abstract Role | Generated API Prefix |
| :--- | :--- | :--- | :--- |
{% for dev in devices %}
| `{{ dev.name }}` | `{{ dev.type }}` | `{{ dev.role or 'N/A' }}` | `{{ dev.name }}_*` (Role default; DAL escape optional) |
{% endfor %}

---

## 2. Configuration Macros

These macros are exported in `device_tree.h` for configuration settings:

{% if config_macros %}
{% for macro in config_macros %}
- `{{ macro }}`
{% endfor %}
{% else %}
*No custom configuration macros exported.*
{% endif %}

---

## 3. Device API Reference

{% for dev in devices %}
### 3.{{ loop.index }} Device: `{{ dev.name }}`

- **Driver Type:** `{{ dev.type }}`
- **Role:** `{{ dev.role or 'N/A' }}`

{% if dev.role %}
#### Available Role APIs:

{% if dev.role == 'binary_indicator' %}
- **`void {{ dev.name }}_activate(void)`**
  Activate/turn on the device (e.g., turn on LED).
- **`void {{ dev.name }}_deactivate(void)`**
  Deactivate/turn off the device (e.g., turn off LED).
- **`void {{ dev.name }}_toggle(void)`**
  Toggle the state of the device.

{% elif dev.role == 'binary_sensor' %}
- **`bool {{ dev.name }}_is_active(void)`**
  Check if the sensor is active. Returns `false` on error.
- **`wink_status_t {{ dev.name }}_is_active_status(bool *out_active)`** (⚠️ Return value must be checked)
  Contracts-honest version to check sensor status.
- **`bool {{ dev.name }}_was_active(void)`**
  Detect rising-edge transition. Returns `false` on error.
- **`wink_status_t {{ dev.name }}_was_active_status(bool *out_pressed)`** (⚠️ Return value must be checked)
  Contracts-honest version to detect rising-edge transition.
- **`wink_status_t {{ dev.name }}_enable_events(void)`** (⚠️ Return value must be checked) — **L1 default**
  Start posting button events to the event queue (period from JSON `auto_poll_ms`).
- **`void {{ dev.name }}_disable_events(void)`** — **L1 default**
  Stop posting button events.
- **`wink_status_t {{ dev.name }}_start_auto_poll(uint32_t poll_ms)`** (⚠️ Return value must be checked) — L2
  Start periodic software timer with an explicit poll period.
- **`void {{ dev.name }}_stop_auto_poll(void)`** — L2
  Stop periodic software timer.

{% elif dev.role == 'distance_sensor' %}
- **`wink_status_t {{ dev.name }}_request_measurement(void)`** (⚠️ Return value must be checked)
  Trigger a non-blocking measurement.
- **`float {{ dev.name }}_read_distance(void)`**
  Get cached distance measurement (returns `-1.0f` on error).
- **`wink_status_t {{ dev.name }}_read_distance_status(float *out_dist_cm)`** (⚠️ Return value must be checked)
  Contracts-honest get distance measurement.
- **`wink_status_t {{ dev.name }}_enable_distance_events(void)`** (⚠️ Return value must be checked) — **L1 default**
  Start posting `WINK_EVENT_DISTANCE_READY` each completed measurement (period from JSON `auto_poll_ms`, default 50).
- **`void {{ dev.name }}_disable_distance_events(void)`** — **L1 default**
  Stop posting distance events.

{% elif dev.role == 'angular_actuator' %}
- **`void {{ dev.name }}_set_angle(float angle)`**
  Set servo angle in degrees.

{% elif dev.role == 'text_display' %}
- **`void {{ dev.name }}_clear(void)`**
  Clear screen buffer.
- **`void {{ dev.name }}_draw_text(uint16_t x, uint8_t y, const char *str)`**
  Draw text on buffer.
- **`void {{ dev.name }}_flush(void)`**
  Flush screen buffer to physical display.

{% else %}
- *Custom role wrappers.*
{% endif %}

{% else %}
#### Custom Device Access:
This device does not have a high-level Role wrapper. Access it directly using its global handle:
```c
extern {{ dev.device_type }} {{ dev.name }};
```
And call its DAL APIs (e.g., `dal_{{ dev.type }}_*` functions declared in `{{ dev.headers[0] if dev.headers else "dal_" + dev.type + ".h" }}`).
{% endif %}

---
{% endfor %}
