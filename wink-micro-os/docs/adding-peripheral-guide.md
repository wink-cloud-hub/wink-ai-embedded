# ???????????ADR-0046?

???? [ADDING_PERIPHERAL.md](file:///d:/workspaces/ai-coding/wink-ai/wink-ai/packages/unisim/docs/ADDING_PERIPHERAL.md) ????? `type` ???**????????**?

?????[tech-design 2026-07-27](../../docs/design/tech-designs/2026-07-27-peripheral-onboarding-optimization-design.md)?[ADR-0046](../../docs/design/decisions/0046-dal-driver-registry-ssot.md)?

---

## 1. ??? SSOT

```text
wink.py new-dal ? dal_*.{h,c} + drivers/<type>.py
                         ?
              DriverBase registry??????
                         ?
         list_drivers.py --cmake --mode=source|defs
                         ?
         CMake ??? foreach????????
```

**SSOT** = `wink-tools/tools/codegen/drivers/*.py`?`list_drivers` / `boards/` / ????????? codegen ????????? cwd????? `dal/CMakeLists.txt` / `wink_dal_drivers.cmake` / Binary SDK / `ALL_WINK_USE_OPTIONS` ???????

---

## 2. `wink.py new-dal`

```text
python wink-tools/wink.py new-dal <type> \
  --category <input|output|actuator|sensor|display|communication|storage> \
  [--actuator] [--role <name>] [--pin-field <name>]... [--force]
```

???

| ?? | ?? |
|------|------|
| DAL ? | `wink-micro-os/dal/include/<category>/dal_<type>.h` |
| DAL ? | `wink-micro-os/dal/src/<category>/dal_<type>.c` |
| Codegen ?? | `wink-tools/tools/codegen/drivers/<type>.py` |

??????? configure CMake?`WINK_KNOWN_DRIVERS` ?????????

---

## 3. ????

```text
python wink-tools/wink.py lint --pack drivers --pack layering --pack api
python wink-tools/tools/codegen/list_drivers.py --check
python wink-tools/tools/codegen/list_drivers.py --json   # ??????
python wink-tools/wink.py build host --app <your_app>
```

Host ??????? `wink-app.json` ??????? JSON????????? stub ???

---

## 4. Unisim ???????

??? Manifest ? `type` ?????? codegen `DriverBase.type` **????**?????? CI?????? `list_drivers.py --json` ??????

---

## 5. ?????????

| ?? / ?? | ?? |
|-------------|------|
| `WINK_TOOLS_ROOT` | `?/wink-tools`?CLI / codegen / lint? |
| `WINK_MICRO_OS_ROOT` | `?/wink-micro-os`?C ???? |
| ?? | `python wink-tools/wink.py ?` |
| ?? shim | `wink-micro-os/tools/wink.py` ? ?????????? |
