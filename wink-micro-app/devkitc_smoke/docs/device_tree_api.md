# DEVKITC_SMOKE Device Tree & API Specification

This document is automatically generated by the Wink codegen tool from `wink-app.json`.
Do not edit this file directly.

- **Board:** `esp32_devkitc`
- **Source Configuration:** `D:/workspaces/ai-coding/wink-ai/wink-ai-embedded/wink-micro-app/devkitc_smoke/wink-app.json`

---

## 1. Device Summary

| Device Name | Driver Type | Abstract Role | Generated API Prefix |
| :--- | :--- | :--- | :--- |
| `board_led` | `led` | `binary_indicator` | `board_led_*` (or direct DAL calls) |
| `boot_button` | `button` | `binary_sensor` | `boot_button_*` (or direct DAL calls) |
| `smoke_sonar` | `ultrasonic` | `distance_sensor` | `smoke_sonar_*` (or direct DAL calls) |

---

## 2. Configuration Macros

These macros are exported in `device_tree.h` for configuration settings:

- `#define BOARD_LED_ACTIVE_HIGH true`
- `#define BOOT_BUTTON_AUTO_POLL_MS 10u`
- `#define BOOT_BUTTON_LONG_PRESS_MS 3000u`
- `#define SMOKE_SONAR_USE_RMT true`

---

## 3. Device API Reference

### 3.1 Device: `board_led`

- **Driver Type:** `led`
- **Role:** `binary_indicator`

#### Available Role APIs:

- **`void board_led_activate(void)`**
  Activate/turn on the device (e.g., turn on LED).
- **`void board_led_deactivate(void)`**
  Deactivate/turn off the device (e.g., turn off LED).
- **`void board_led_toggle(void)`**
  Toggle the state of the device.



---
### 3.2 Device: `boot_button`

- **Driver Type:** `button`
- **Role:** `binary_sensor`

#### Available Role APIs:

- **`bool boot_button_is_active(void)`**
  Check if the sensor is active. Returns `false` on error.
- **`wink_status_t boot_button_is_active_status(bool *out_active)`** (⚠️ Return value must be checked)
  Contracts-honest version to check sensor status.
- **`bool boot_button_was_active(void)`**
  Detect rising-edge transition. Returns `false` on error.
- **`wink_status_t boot_button_was_active_status(bool *out_pressed)`** (⚠️ Return value must be checked)
  Contracts-honest version to detect rising-edge transition.
- **`wink_status_t boot_button_start_auto_poll(uint32_t poll_ms)`** (⚠️ Return value must be checked)
  Start periodic software timer for polling the button state.
- **`void boot_button_stop_auto_poll(void)`**
  Stop periodic software timer.



---
### 3.3 Device: `smoke_sonar`

- **Driver Type:** `ultrasonic`
- **Role:** `distance_sensor`

#### Available Role APIs:

- **`wink_status_t smoke_sonar_request_measurement(void)`** (⚠️ Return value must be checked)
  Trigger a non-blocking measurement.
- **`float smoke_sonar_read_distance(void)`**
  Get cached distance measurement (returns `-1.0f` on error).
- **`wink_status_t smoke_sonar_read_distance_status(float *out_dist_cm)`** (⚠️ Return value must be checked)
  Contracts-honest get distance measurement.



---
