{
  "header": {
    "version": "3.0",
    "name": "mcs51 CMS8S78xx on-chip temperature sensor calibration and sampling headless proof",
    "templateId": "vendor_cms8s78xx_v202_temperture_sensor",
    "accuracyMode": "behavioral",
    "timeoutUs": "500000",
    "failurePolicy": "fail-fast",
    "determinism": { "prngSeed": 42 }
  },
  "steps": [
    {
      "type": "ASSERT_POINT",
      "timeUs": "100ms",
      "target": "gpio:26",
      "matcher": 0,
      "description": "[微调完成] TS_Adjust() 逐档扫描并在 1365 LSB 窗口内收敛锁定 TS_REG 最优微调档位，退出后执行 P32=0 (pin 26) 点亮板载指示灯，断言 P3.2 输出低电平 0"
    },
    {
      "type": "ASSERT_POINT",
      "timeUs": "300ms",
      "target": "gpio:26",
      "matcher": 0,
      "description": "[稳定运行] 进入 while(1) 循环持续调用 TS_GetTemperature() 采样解算 TsValue，P3.2 稳定保持为 0，程序无死锁"
    }
  ]
}
