# DUAL_TASK_DEMO Device Tree & API Specification

This document is automatically generated by the Wink codegen tool from `wink-app.json`.
Do not edit this file directly.

**Default path (L1 / A+B):** use Role APIs `{name}_{verb}` below.
**Expert escape hatch (L2 / C):** `extern dal_*_t` instances + `dal_*` named APIs remain available; prefer Role wrappers unless you need pointer-passing or advanced control.

- **Board:** `esp32_devkitc`
- **Source Configuration:** `D:/MyWorkSpace_program/lowcode-nocode/ai-app/wink-ai-embedded/wink-micro-app/dual_task_demo/wink-app.json`

---

## 1. Device Summary

| Device Name | Driver Type | Abstract Role | Generated API Prefix |
| :--- | :--- | :--- | :--- |
| `front_radar` | `ultrasonic` | `distance_sensor` | `front_radar_*` (Role default; DAL escape optional) |
| `neck_servo` | `rc_servo` | `angular_actuator` | `neck_servo_*` (Role default; DAL escape optional) |

---

## 2. Configuration Macros

These macros are exported in `device_tree.h` for configuration settings:

- `#define FRONT_RADAR_AUTO_POLL_MS 50u`
- `#define NECK_SERVO_MIN_PULSE_US 500u`
- `#define NECK_SERVO_MAX_PULSE_US 2500u`

---

## 3. Device API Reference

### 3.1 Device: `front_radar`

- **Driver Type:** `ultrasonic`
- **Role:** `distance_sensor`

#### Available Role APIs:

- **`wink_status_t front_radar_request_measurement(void)`** (⚠️ Return value must be checked)
  Trigger a non-blocking measurement.
- **`float front_radar_read_distance(void)`**
  Get cached distance measurement (returns `-1.0f` on error).
- **`wink_status_t front_radar_read_distance_status(float *out_dist_cm)`** (⚠️ Return value must be checked)
  Contracts-honest get distance measurement.
- **`wink_status_t front_radar_enable_distance_events(void)`** (⚠️ Return value must be checked) — **L1 default**
  Start posting `WINK_EVENT_DISTANCE_READY` each completed measurement (period from JSON `auto_poll_ms`, default 50).
- **`void front_radar_disable_distance_events(void)`** — **L1 default**
  Stop posting distance events.



---
### 3.2 Device: `neck_servo`

- **Driver Type:** `rc_servo`
- **Role:** `angular_actuator`

#### Available Role APIs:

- **`void neck_servo_set_angle(float angle)`**
  Set servo angle in degrees.



---
