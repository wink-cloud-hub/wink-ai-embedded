# OLED_DASHBOARD Device Tree & API Specification

This document is automatically generated by the Wink codegen tool from `wink-app.json`.
Do not edit this file directly.

**Default path (L1 / A+B):** use Role APIs `{name}_{verb}` below.
**Expert escape hatch (L2 / C):** `extern dal_*_t` instances + `dal_*` named APIs remain available; prefer Role wrappers unless you need pointer-passing or advanced control.

- **Board:** `esp32_devkitc`
- **Source Configuration:** `wink-micro-app/oled_dashboard/wink-app.json`

---

## 1. Device Summary

| Device Name | Driver Type | Abstract Role | Generated API Prefix |
| :--- | :--- | :--- | :--- |
| `user_button` | `button` | `binary_sensor` | `user_button_*` (Role default; DAL escape optional) |
| `status_led` | `led` | `binary_indicator` | `status_led_*` (Role default; DAL escape optional) |
| `status_oled` | `ssd1306` | `text_display` | `status_oled_*` (Role default; DAL escape optional) |

---

## 2. Configuration Macros

These macros are exported in `device_tree.h` for configuration settings:

- `#define USER_BUTTON_DEBOUNCE_MS 20u`
- `#define USER_BUTTON_EVENT_DRIVE_SOFT_POLL true`
- `#define USER_BUTTON_AUTO_POLL_MS 10u`
- `#define STATUS_LED_ACTIVE_HIGH true`
- `#define STATUS_OLED_I2C_ADDR 0x3C`

---

## 3. Device API Reference

### 3.1 Device: `user_button`

- **Driver Type:** `button`
- **Role:** `binary_sensor`

#### Available Role APIs:

- **`bool user_button_is_active(void)`**
  Check if the sensor is active. Returns `false` on error.
- **`wink_status_t user_button_is_active_status(bool *out_active)`** (⚠️ Return value must be checked)
  Contracts-honest version to check sensor status.
- **`bool user_button_was_active(void)`**
  Detect rising-edge transition. Returns `false` on error.
- **`wink_status_t user_button_was_active_status(bool *out_pressed)`** (⚠️ Return value must be checked)
  Contracts-honest version to detect rising-edge transition.
- **`wink_status_t user_button_enable_events(void)`** (⚠️ Return value must be checked) — **L1 default**
  Start posting button events to the event queue (period from JSON `auto_poll_ms`).
- **`void user_button_disable_events(void)`** — **L1 default**
  Stop posting button events.
- **`wink_status_t user_button_start_auto_poll(uint32_t poll_ms)`** (⚠️ Return value must be checked) — L2
  Start periodic software timer with an explicit poll period.
- **`void user_button_stop_auto_poll(void)`** — L2
  Stop periodic software timer.



---
### 3.2 Device: `status_led`

- **Driver Type:** `led`
- **Role:** `binary_indicator`

#### Available Role APIs:

- **`void status_led_activate(void)`**
  Activate/turn on the device (e.g., turn on LED).
- **`void status_led_deactivate(void)`**
  Deactivate/turn off the device (e.g., turn off LED).
- **`void status_led_toggle(void)`**
  Toggle the state of the device.



---
### 3.3 Device: `status_oled`

- **Driver Type:** `ssd1306`
- **Role:** `text_display`

#### Available Role APIs:

- **`void status_oled_clear(void)`**
  Clear screen buffer.
- **`void status_oled_draw_text(uint16_t x, uint8_t y, const char *str)`**
  Draw text on buffer.
- **`void status_oled_flush(void)`**
  Flush screen buffer to physical display.



---
